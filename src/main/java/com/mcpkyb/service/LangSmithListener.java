package com.mcpkyb.service;

/**
 * Marker interface for LangSmith listeners to avoid Spring dependency resolution issues.
 */
public interface LangSmithListener {
    // Marker interface - implementations will be ChatModelListener instances
}
